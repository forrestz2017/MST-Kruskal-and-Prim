/***********************************************************
 * Edge.cpp
 * Implementation of the Edge struct's methods (if any).
 * For this example, we only have an inline operator in Edge.h,
 * so this file may remain empty or omitted altogether.
 ***********************************************************/

#include "Edge.h"

 // The implementation is already done inline in the header, so nothing is needed here.
